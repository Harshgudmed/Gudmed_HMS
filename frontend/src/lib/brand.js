// Central place for hospital branding (name + logo).
import logo from '@/assets/company-logo.jpg'

export const HOSPITAL_NAME = 'Gudmed Super Speciality Hospital'

// Company logo (bundled by Vite). To change it, replace
// src/assets/company-logo.jpg. If the image ever fails to load, the
// Logo component falls back to a "GS" monogram automatically.
export const HOSPITAL_LOGO = logo
