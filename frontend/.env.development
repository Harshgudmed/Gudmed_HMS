# VITE_API_URL=http://localhost:5000/api
# ↑ Commented for tunnel/remote demo: frontend now uses the relative "/api" path,
#   which Vite proxies to localhost:5000 — so a Cloudflare tunnel works with ONE tunnel
#   (no CORS, no backend tunnel). Uncomment to go back to normal local dev.
